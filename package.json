{
  "name": "life-management-app",
  "version": "1.0.0",
  "description": "Complete Life Management App - Frontend and Backend",
  "main": "frontend/build/index.html",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "mongoose": "^6.9.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "express-validator": "^6.14.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "jest": "^29.4.0"
  },
  "keywords": ["life-management", "productivity", "fullstack", "react", "nodejs"],
  "author": "Julian Malvestuto",
  "license": "ISC",
  "engines": {
    "node": "18.x"
  }
}
